using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Types
{
    class Program
    {
        static void Main(string[] args)
        {
            float testFloat = 1.2f; // This is suffixed with 'f' , otherwise compiler will not allow.
            decimal testDecimal = 1.2M;// This is suffixed with 'M' , otherwise compiler will not allow.
            Console.Read();
        }
    }
}
